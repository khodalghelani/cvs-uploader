<%= form_with(model: t, local: true) do |form| %>
  <% if t.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(t.errors.count, "error") %> prohibited this t from being saved:</h2>

      <ul>
      <% t.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :t_title %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description, id: :t_description %>
  </div>

  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author, id: :t_author %>
  </div>

  <div class="field">
    <%= form.label :tags %>
    <%= form.text_field :tags, id: :t_tags %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
